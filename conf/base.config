params {

    help = false

    // Run options
    num_cores = 8
    bin_dir = "${projectDir}/bin"
    ftcaller = 'both'
    hla_typing = false
    hla_only = false

    trim_reads_for_hla = false
    trim_reads_for_main = false

    // Input and output directories
    input_dir = false
    output_dir = false
    hla_typing_dir = false

    // Containers for submodules
    container__preproc = "sufyazi/preproc-crmy"
    container__hlahd = "sufyazi/hlahd-crmy"
    container__arriba = "sufyazi/arriba-crm"
    container__fuscat = "sufyazi/fusioncatcher-crm"
    container__agfusion = "sufyazi/agfusion-crm"
    container__pypolars = "sufyazi/pypolars-crm"
    container__spladder = "sufyazi/spladder-crm"
}